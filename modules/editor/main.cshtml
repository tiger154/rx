@{
    /*****************************************************************
    //
    // Programmed by SungHO Kim (Migration: TaiHyung Kim, YoonA Shim)
    // KimsQ-Rx (for Microsoft ASP.NET WebPages)
    // Copyrights © Redblock Allrights Reserved. Powered by kimsQ-Rx
    // Since 2011.
    //
    *****************************************************************/
    
    var d_module = new Dictionary<string, string>();
    
    //////////////////////////////////////////////////////////////////
    /// Get PageDate
    //////////////////////////////////////////////////////////////////
    var g = PageData[0];
    var my = PageData[1];
    var _HM = PageData[2];
    var _HP = PageData[3];
    var system = PageData[4];
    var _page = PageData[5];
    var m = PageData[6];
    var bid = PageData[7];
    var _mod = PageData[8];
    var referer = PageData[9];
    var skin = PageData[10];
    var _HS = PageData[11];
    var s = PageData[12];
    var c = PageData[13];
    var p = PageData[14];
    var r = PageData[15];
    var front = PageData[16];
    var _m = PageData[17];

    //////////////////////////////////////////////////////////////////
    /// Get Request
    //////////////////////////////////////////////////////////////////
    //var front = Request["front"];
    //var skin = Request["skin"];
    var compo = Request["compo"];

    string iframe = "Y";
    string mod = "main";
    string g_main;

    dynamic d_editor = _DB.Get_code("editor");
    
    if(front == "lib") {
    	g_main = g["dir_module"]+front+"/"+compo+".cshtml";
    }
    else {
        if(skin != null && skin != "") 
        {
            d_module["skin"] = skin;          
		}
        else
        {
            d_module["skin"] = d_editor["skin"];
        }

        // global 변수 세팅
        g["dir_module_skin"] = g["dir_module"]+"skin\\"+d_module["skin"]+"\\";    
        g["url_module_skin"] = g["url_module"]+"/skin/"+d_module["skin"]+"/";    
        g["img_module_skin"] = g["url_module_skin"]+"/image";    

        g["dir_module_mode"] = g["dir_module_skin"]+mod;    
        g["url_module_mode"] = g["url_module_skin"]+"/"+mod;      
                
	    g_main = g["dir_module_mode"]+".cshtml";
    }

    // 세션 생성.
    Session["g"] = g;
    Session["g_main"] = g_main;
    Session["iframe"] = iframe;
}
