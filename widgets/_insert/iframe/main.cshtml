@{
    /*****************************************************************
    //
    // Programmed by SungHO Kim (Migration: YoonA Shim, TaiHyung Kim)
    // KimsQ-Rx (for Microsoft ASP.NET WebPages)
    // Copyrights © Redblock Allrights Reserved. Powered by kimsQ-Rx
    // Since 2011.
    //
    *****************************************************************/   
    var g = PageData[0];
    var _m = PageData[1];
    var c = PageData[2];
    var m = PageData[3];
    var p = PageData[4];
    var _HS = PageData[5];
    var my = PageData[6];
    var system = PageData[7];
    var d_layout = PageData[8];
    var s = PageData[9];
    var _keyword = PageData[10];
    var _mod = PageData[11];
    var r = PageData[12];
    var _HP = PageData[13];
    var _HM = PageData[14];
    var date = PageData[15];
    var referer = PageData[16];
    var wdgvar = PageData[17];    

    if (!wdgvar.ContainsKey("url"))
    {
        wdgvar.Add("url", "");
    }
    if (!wdgvar.ContainsKey("width"))
    {
        wdgvar.Add("width", "");
    }
    if (!wdgvar.ContainsKey("height"))
    {
        wdgvar.Add("height", "");
    }
}




<embed src="@wdgvar["url"]"@Html.Raw((wdgvar["width"].Trim() != "") ? " width=" + wdgvar["width"] : "")@Html.Raw((wdgvar["height"].Trim() != "") ? " height=" + wdgvar["height"] : "")></embed>
