@{
    /*****************************************************************
    //
    // Programmed by SungHO Kim (Migration: YoonA Shim, TaiHyung Kim)
    // KimsQ-Rx (for Microsoft ASP.NET WebPages)
    // Copyrights © Redblock Allrights Reserved. Powered by kimsQ-Rx
    // Since 2011.
    //
    *****************************************************************/   
    var g = PageData[0];
    var _m = PageData[1];
    var c = PageData[2];
    var m = PageData[3];
    var p = PageData[4];
    var _HS = PageData[5];
    var my = PageData[6];
    var system = PageData[7];
    var d_layout = PageData[8];
    var s = PageData[9];
    var _keyword = PageData[10];
    var _mod = PageData[11];
    var r = PageData[12];
    var _HP = PageData[13];
    var _HM = PageData[14];
    var date = PageData[15];
    var referer = PageData[16];
    var wdgvar = PageData[17];    

    if (!wdgvar.ContainsKey("id"))
    {
        wdgvar.Add("id", "");
    }
    if (!wdgvar.ContainsKey("width"))
    {
        wdgvar.Add("width", "250");
    }
    if (!wdgvar.ContainsKey("height"))
    {
        wdgvar.Add("height", "300");
    }
    if (!wdgvar.ContainsKey("bgcolor1"))
    {
        wdgvar.Add("bgcolor1", "333333");
    }
    if (!wdgvar.ContainsKey("color1"))
    {
        wdgvar.Add("color1", "ffffff");
    }
    if (!wdgvar.ContainsKey("bgcolor"))
    {
        wdgvar.Add("bgcolor", "000000");
    }
    if (!wdgvar.ContainsKey("color"))
    {
        wdgvar.Add("color", "ffffff");
    }
    if (!wdgvar.ContainsKey("num"))
    {
        wdgvar.Add("num", "10");
    }
    if (!wdgvar.ContainsKey("link"))
    {
        wdgvar.Add("link", "4aed05");
    }
    if (!wdgvar.ContainsKey("useScroll"))
    {
        wdgvar.Add("useScroll", "1");
    }
    if (!wdgvar.ContainsKey("useloof"))
    {
        wdgvar.Add("useloof", "1");
    }  
    
    bool useScroll = false;
    bool useloof = false;
    
    if (wdgvar["useScroll"] == "1")
    {
        useScroll = true;
    }
    if (wdgvar["useloof"] == "1")
    {
        useloof = true;
    }
    
    
}


@Twitter.Profile(wdgvar["id"], Convert.ToInt32(wdgvar["width"]), Convert.ToInt32(wdgvar["height"]), "#" + wdgvar["bgcolor1"], "#" + wdgvar["color1"], "#" + wdgvar["bgcolor"], "#" + wdgvar["color"], "#" + wdgvar["link"], Convert.ToInt32(wdgvar["num"]), useScroll, useloof, false, true, true, false, Twitter.WidgetBehaviors.All, 6)

